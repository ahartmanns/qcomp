Guidelines for contributions:
- Use tabs for indentation in the .json files.
- Use the short name of the model for the file names of all model files (original and converted) wherever possible.
- The names of result files should be the name of the tool, followed by a dot, followed by the parameter values separated by dashes in the order in which they appear on the website, followed by the file extension.